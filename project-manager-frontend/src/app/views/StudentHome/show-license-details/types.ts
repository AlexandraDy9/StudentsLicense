import { Student } from '../../../models/Student';

export interface ShowLicenseDetailsProps {
  readonly literals: any;
  readonly student: Student;
  readonly createMode?: boolean;
}

export interface HandlerProps {
}
